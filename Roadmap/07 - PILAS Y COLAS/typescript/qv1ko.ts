let stack: number[] = [];

stack.push(1);
stack.push(2);
stack.push(3);

if (stack.length !== 0) {
    console.log(stack.pop());
}

let queue: string[] = [];

queue.push('a');
queue.push('b');
queue.push('c');

if (queue.length !== 0){
    console.log(queue[0]);
}
