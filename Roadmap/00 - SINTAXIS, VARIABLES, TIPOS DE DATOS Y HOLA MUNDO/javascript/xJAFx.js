// https://developer.mozilla.org/es/docs/Web/JavaScript

// Comentario de una línea

/* Comentario
para
varias
líneas */

let posibleAlterar = "Esta es una variable posible de alterar tras ser declarada. Puede ser declarada sin asignarle un valor";

const imposibleAlterar = "Esta es una constante, variable imposible de alterar tras ser declarada. Debe ser declarada con su valor respectivo";

let interruptor = true;
let nulo = null;
let sinDefinir;
let numero = 20;
let numeroGrande = 73250731234720572n;
let textos = "Variable para textos";
let simbolo = Symbol();

console.log("¡Hola, JavaScript!");