// imprime nº del 1 al 10 con bucle for.

for (let index = 1; index < 11; index++) {
    console.log (index);
}

// imprime nº del 1 al 10 con bucle while.

let index = 0;

while (index <= 9) {
    index += 1
    console.log (index);
}


// imprime nº del 1 al 10 con bucle do

let index = 1;

do {
    console.log (index);
    index += 1;
} while (index <= 10);
